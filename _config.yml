title:               PISE notes
include:             ['_pages']
exclude:             ['_includes/notes_graph.json']
baseurl:             /
url:                 "https://pise-notes.tk"
description: >-
  Notes from PISE classes at Ca' Foscari university in Venice
permalink:           pretty
relative_permalinks: false

plugins:
  - jekyll-last-modified-at
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-target-blank
  - jekyll-redirect-from
  - jekyll-optional-front-matter

sass:
  sass_dir: _sass
  style: :compressed

collections:
  notes:
    output: true
    permalink: /:slug

defaults:
  - scope:
      path: "**/*"
    values:
      layout: "default"
  - scope:
      path: "_notes/**/*.md"
    values:
      layout: "note"
